# 可执行文件（仅忽略仓库根目录的构建产物，不忽略 cmd/runner-manager、cmd/runner-agent 源码）
/runner-manager
/runner-agent
*.exe

# Runner 数据目录（按需取消注释）
# runners/
# 各 runner 目录下的 GitHub 检查 token（若 runners/ 被纳入版本库请取消下一行注释）
# **/.github_check_token

# 本地配置（若含敏感信息建议加入）
config.yaml
.env

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# 系统
.DS_Store
Thumbs.db

.cursor/
